ENC Parser GeoJSON

**ENC Parser GeoJSON** is a Python package for parsing **S-57 Electronic Navigational Chart (ENC)** files and exporting selected layers and features as **GeoJSON**. It’s designed for marine spatial applications, machine learning preprocessing, and semantic feature extraction.

---

## Features

- Parse S-57 ENC `.000` files using GDAL/OGR
- Extract layers and features with metadata
- Export to GeoJSON format
- Unit-tested and modular for reuse

---

## Installation

### 1. Clone the Repository

```bash
git clone https://github.com/your-username/enc_parser_geojson.git
cd enc_parser_geojson
python -m main


## Install Dependencies

### 1. pip install -r requirements.txt

### 2. Or install as a package: pip install .